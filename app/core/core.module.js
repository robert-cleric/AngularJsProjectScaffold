(function () {
    'use strict';

    angular
        .module('app.core', [
            /*
             * Reusable cross app code modules
             */
            'blocks.logger',
            'blocks.exception'

        ]);

})();