version: '2.1'

services:
  angular-docker:
    image: angular-docker
    build: .
    environment:
      NODE_ENV: production
    ports:
      - 80:443
    ## set your startup file here
    command: [nginx-debug, '-g', 'daemon off;']